# ============================================================================
# CIBN DIGITAL LIBRARY - PRODUCTION ENVIRONMENT VARIABLES
# ============================================================================
# IMPORTANT: Copy this file to .env and update with your production values
# DO NOT commit .env file with actual credentials to git
# ============================================================================

# ----------------------------------------------------------------------------
# SERVER CONFIGURATION
# ----------------------------------------------------------------------------
NODE_ENV=production
PORT=8000

# ----------------------------------------------------------------------------
# DATABASE CONFIGURATION (PostgreSQL)
# ----------------------------------------------------------------------------
# Docker Compose will use these to create the database
POSTGRES_USER=cibn_user
POSTGRES_PASSWORD=Valerian101!
POSTGRES_DB=cibn_library

# Full database URL (used by backend)
# Format: postgresql://username:password@host:port/database
# For Docker: use service name 'postgres' as host
DATABASE_URL=postgresql://cibn_user:Valerian101!@postgres:5432/cibn_library

# ----------------------------------------------------------------------------
# SECURITY
# ----------------------------------------------------------------------------
# Generate a secure secret key: openssl rand -hex 32
SECRET_KEY=CHANGE_THIS_TO_RANDOM_SECRET_KEY_MIN_32_CHARS

# ----------------------------------------------------------------------------
# CORS CONFIGURATION
# ----------------------------------------------------------------------------
# For production, specify your actual domain(s)
# Format: JSON array of allowed origins
CORS_ORIGINS=["https://yourdomain.com", "https://www.yourdomain.com"]

# ----------------------------------------------------------------------------
# FRONTEND URL
# ----------------------------------------------------------------------------
# Your production frontend URL (for payment callbacks, emails, etc.)
FRONTEND_URL=https://yourdomain.com

# ----------------------------------------------------------------------------
# NEXT.JS FRONTEND API URL
# ----------------------------------------------------------------------------
# URL where frontend will call the backend API
# For Docker: use http://backend:8000 for internal communication
# For external access: use your domain or http://your-server-ip:8000
NEXT_PUBLIC_API_URL=https://api.yourdomain.com

# ----------------------------------------------------------------------------
# CIBN MEMBER DATABASE (External MS SQL Server)
# ----------------------------------------------------------------------------
# Credentials for the existing CIBN member database
CIBN_DB_SERVER=your-mssql-server-address
CIBN_DB_DATABASE=cibn_members_db_name
CIBN_DB_USERNAME=cibn_db_user
CIBN_DB_PASSWORD=cibn_db_password

# ----------------------------------------------------------------------------
# PAYMENT GATEWAY (Paystack)
# ----------------------------------------------------------------------------
# Get from: https://dashboard.paystack.com/#/settings/developer
PAYSTACK_SECRET_KEY=sk_live_your_paystack_secret_key

# ----------------------------------------------------------------------------
# FILE UPLOADS
# ----------------------------------------------------------------------------
# Directory for uploaded files (inside container)
UPLOAD_DIR=/app/uploads

# ----------------------------------------------------------------------------
# OPTIONAL: SSL/TLS (if using HTTPS)
# ----------------------------------------------------------------------------
# SSL_CERT_PATH=/etc/nginx/ssl/cert.pem
# SSL_KEY_PATH=/etc/nginx/ssl/key.pem

# ============================================================================
# NOTES FOR PRODUCTION DEPLOYMENT:
# ============================================================================
# 1. Change all "CHANGE_THIS_*" values to secure production values
# 2. Use strong passwords (minimum 16 characters, mixed case, numbers, symbols)
# 3. Generate SECRET_KEY using: openssl rand -hex 32
# 4. Update CORS_ORIGINS with your actual domain(s)
# 5. Update FRONTEND_URL and NEXT_PUBLIC_API_URL with your domain
# 6. Configure CIBN_DB_* with your actual MS SQL Server credentials
# 7. Add your Paystack live secret key (starts with sk_live_)
# 8. Never commit .env file to git repository
# 9. Keep backup of .env file in a secure location
# 10. Use different passwords for dev/staging/production
# ============================================================================
